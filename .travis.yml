language: node_js
node_js:
  - '10'
cache:
  yarn: true
  directories:
    - 'node_modules'
branches:
  only:
    - 'master'
jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: release
      node_js: '10'
      script: 'yarn release'
